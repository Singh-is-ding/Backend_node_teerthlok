services:
  - type: web
    name: vr-video-server
    env: node
    buildCommand: apt-get update && apt-get install -y ffmpeg && npm install && pip install -U yt-dlp
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
